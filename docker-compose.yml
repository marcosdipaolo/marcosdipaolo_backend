# For more information: https://laravel.com/docs/sail
version: '3'
services:
    laravel.test:
        build: .
        ports:
            - '8989:80'
        volumes:
            - '.:/var/www/html'
